name: Weekly codegen refresh

on:
  schedule:
    # Every Monday at 03:00 UTC
    - cron: "0 3 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: weekly-codegen-refresh
  cancel-in-progress: false

jobs:
  regenerate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: dev

      - name: Create .env
        run: echo 'MAIN_FOLDER_NAME="anilibria_api_types"' > .env

      - name: Setup Poetry and dependencies
        uses: ./.github/actions/setup-poetry

      - name: Regenerate codegen
        run: poetry run python -m anilibria_api_types.main

      - name: Apply Ruff to generated code
        run: |
          poetry run ruff check --fix --unsafe-fixes --exit-zero anilibria_api_types/codegen
          poetry run ruff format anilibria_api_types/codegen || true

      - name: Show diff
        run: git status --short

      - name: Create pull request
        uses: peter-evans/create-pull-request@v6
        with:
          add-paths: anilibria_api_types/codegen
          commit-message: "chore: regenerate codegen"
          branch: codegen/refresh
          base: dev
          title: "chore: regenerate codegen"
          labels: automated-pr
          body: |
            Weekly automatic regeneration of the AniLibria codegen output.

            If you're seeing this PR, the upstream OpenAPI schema changed and the
            committed generated files under `anilibria_api_types/codegen/` are now
            out of date.
          delete-branch: true
